#Day 1 - Python Data Layer
#create a Python program that can query the database using code.
#
# Note: A flight is considered delayed if it is delayed by 20 minutes or more.
# Note: An empty value should not be treated as a delay.

#Create a Data Access Layer (DAL)
#Your goal is to build a Data Access Layer — a set of functions that interact with the database and return query results.